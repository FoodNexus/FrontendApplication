package tn.esprit.msvalorisationeconomiecirculaire.dto;

/** Aligné sur {@code recycler-operations.model.ts} (Angular). */
public enum RequestStatus {
    awaiting_donor,
    pending,
    approved,
    available,
    done,
    rejected,
    pending_verification,
    verified,
    verification_rejected
}
